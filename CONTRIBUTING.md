# Contributing

Use Python 3.12+, Node.js LTS, Docker Compose, and the commands in the README.
Keep changes focused, add tests, run formatting/lint/type checks, and never
commit credentials or sensitive payloads. Security-sensitive changes require a
clear threat-boundary explanation in the pull request.

